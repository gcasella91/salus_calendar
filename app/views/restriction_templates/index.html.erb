<div class="row mb-3">
  <div class="col-md-12">
    <h1>
      All restrictions
    </h1>

    <a href="/restrictions/new" class="btn btn-block btn-outline-secondary">
      Add a new restriction
    </a>
  </div>
</div>

<!-- Ransack Code start -->

<!-- Ransack Code end -->

<!-- Location Map Code start -->

<!-- Location Map Code end -->

<hr>

<div class="row mb-3">
  <div class="col-md-12">
    <table class="table">
      <tr>
        <th>
          ID
        </th>

        <th>
          User
        </th>

        <th>
          Created at
        </th>

        <th>
          Updated at
        </th>

        <th>
        </th>
      </tr>

      <% @restrictions.each do |restriction| %>
      <tr>
        <!-- Display restriction.id start -->
        <td>
          <%= restriction.id %>
        </td>
        <!-- Display restriction.id end -->

        <!-- Display restriction.user_id start -->
        <td>
          <% if restriction.user.present? %>
            <a href="/users/<%= restriction.user.id %>">
              <%= restriction.user.token_calendar %>
            </a>
          <% end %>
        </td>
        <!-- Display restriction.user_id end -->

        <!-- Display restriction.created_at start -->
        <td>
          <%= time_ago_in_words(restriction.created_at) %> ago
        </td>
        <!-- Display restriction.created_at end -->

        <!-- Display restriction.updated_at start -->
        <td>
          <%= time_ago_in_words(restriction.updated_at) %> ago
        </td>
        <!-- Display restriction.updated_at end -->

        <td>
          <a href="/restrictions/<%= restriction.id %>">
            Show details
          </a>
        </td>
      </tr>
      <% end %>
    </table>

    <div class="row mb-3">
      <div class="col-md-12">
        <%= paginate @restrictions, theme: "twitter-bootstrap-4" %>
      </div>
    </div>

  </div>
</div>
